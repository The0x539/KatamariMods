name: Build
on:
  - push
  - workflow_dispatch
jobs:
  build:

    runs-on: windows-latest

    steps:
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
      
      - uses: actions/checkout@v6

      - run: dotnet build